name: Run Municipio E2E Tests sharded
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  test:
    strategy:
      matrix:
        shard: [1, 2, 3, 4]
        total-shards: [4]
    runs-on: ubuntu-latest
    steps:
      - name: Run Municipio E2E Tests
        uses: helsingborg-stad/municipio-e2e-action@dev
        with:
          sitemap-urls: 'https://visithelsingborg.hbgtest.se/sitemap.xml?pw_test='
          shard: ${{ matrix.shard }}
          total-shards: ${{ matrix.total-shards }}